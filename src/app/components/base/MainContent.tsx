import Image from "next/image";

import { SearchIcon, HomeIcon, HomeActiveIcon } from "../svg/Icons";

const MainContent = () => {
  return (
    <div className="flex flex-col bg-background-base rounded-xl flex-1 mt-2">
      
    </div>
  );
};

export default MainContent;
